# XeBuild patch repository and build tools

Use one of the `make_release*.bat` files first so that the base patches get built
and copied into the version specific folders.

After that the `build_all.bat` in each version's folder should function for
rebuilding only that set of patches.
